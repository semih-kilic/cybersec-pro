# CyberSec Pro - Git Ignore File

# ═══════════════════════════════════════════════════════════════
# SECURITY — never commit these
# AUDIT 2026-08-28: the repo contained BOTH the encrypted database
# backups AND the key that decrypts them. Anyone with repo access
# had the full production database. Keys now live in ~/.secrets/.
# ═══════════════════════════════════════════════════════════════
.env
.env.*
!.env.example
*.pem
*.key
*.crt
*.pfx
*.p12
credentials.json
service-account.json
*.secret
secrets/
.backup-key
**/state.json

# Database dumps & backups (contain real user data + password hashes)
backups/
*.sql.gz.enc
dump-*.sql
*-dump.sql
db-dump*.sql

# === Environment & Config ===
.env.local
.env.*.local
*.local.yml
rust-backend/.env.bak*
*.env.bak.*

# Nested Git Repositories
cs50_final_project/
moneymanager/
n8n-youtube-shorts-automation/

# Database files
*.db
*.sqlite
*.sqlite3
instance/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
develop-eggs/
downloads/
eggs/
.eggs/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp
temp/

# Backup / scratch files
*.backup
*.bak
*.bak-[0-9]*
*.bak.[0-9]*
vscode-backups/

# Node
node_modules/

# Build outputs
dist/
build/
out/
**/target/
*.tsbuildinfo
cybersec-wasm/pkg/

# Compiled binaries (build them, don't commit them)
bin/
agent-binaries/
ngrok
*.exe
*.mp4
*.wasm

# Docker build copies (not committed)
rust-backend/cybersec-proto/
rust-scan-engine/cybersec-proto/

# Keep these
!frontend/src/lib/
!nginx/ssl/.gitkeep
